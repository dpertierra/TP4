<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/txtcreausuario"
        android:layout_marginTop="10px"
        android:hint="Ingrese el nombre de usuario deseado"
        android:textSize="20dp"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/txtcreapass"
        android:layout_marginTop="5px"
        android:hint="Ingrese la password deseada"
        android:inputType="textPassword"
        android:textSize="20dp"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/txtreingresapass"
        android:layout_marginTop="5px"
        android:hint="Vuelva a ingresar la password"
        android:inputType="textPassword"
        android:textSize="20dp"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btnregistrarse"
        android:text="Registarse"
        android:textSize="30dp"
        />

</LinearLayout>